# Generated protected remotes, rebuilt into the allow maps on every
# nodeguard-maps start. Directives:
#   derp                  allowlist current tailscale DERP relay addresses
#   resolve <name>        allowlist current A/AAAA records of a hostname
#   cidr <net>            allowlist a literal network
derp
resolve api.anthropic.com
