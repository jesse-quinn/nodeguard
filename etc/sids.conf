# Responder SID policy. Reloaded live on change. Directives:
#   block <sid>    opt a severity-2+ signature in to blocking
#   ignore <sid>   never block on this signature (noise)
#   udp-ok <sid>   allow a non-TCP signature to block. HAND-PROMOTED ONLY:
#                  add a comment justifying why blind spoofing cannot
#                  trigger this signature before promoting.
